[Unit]
Description=Local dns service, defines a mapping between user defined addresses and IP from local network

[Service]
Type=simple
ExecStart=cd /usr/local/localdns && sudo python3 -m custom_dns

[Install]
WantedBy=default.target